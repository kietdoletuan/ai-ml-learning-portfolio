## Sensor Data Quality Checker

**What it does:**
-Sorts through an array of float readings then classifies them as 
VALID / NULL / ERROR_CODE / OUT_OF_RANGE / using Python loops
-Calculates basic info such as min, max and mean.
